// protectedroute.spec.js
import ProtectedRoute from '../src/components/ProtectedRoute';

describe('ProtectedRoute', () => {
  it('is a function', () => {
    expect(typeof ProtectedRoute).toBe('function');
  });
});